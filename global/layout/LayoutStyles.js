import {makeStyles} from "@mui/styles";

export const LayoutStyles = makeStyles((theme) => ({
    root: {
        display: 'flex',
    },
    paper__shadow: {
        boxShadow: "none",
        borderRadius: "0",
    },
}));