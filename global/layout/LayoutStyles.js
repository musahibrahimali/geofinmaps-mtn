import {makeStyles} from "@material-ui/core";

export const LayoutStyles = makeStyles((theme) => ({
    root: {
        display: 'flex',
    },
    paper__shadow: {
        boxShadow: "none",
        borderRadius: "0",
    },
}));